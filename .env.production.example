# Production Environment Variables
# These values are loaded from AWS Parameter Store via load-parameters.sh

# Cognito Authentication Configuration
COGNITO_CLIENT_ID=67cu7n5lcefipd6moq5eju6m9a
COGNITO_CLIENT_SECRET=<retrieved-from-parameter-store>
COGNITO_ISSUER=https://cognito-idp.eu-north-1.amazonaws.com/eu-north-1_6ruju9nUv

# NextAuth Configuration
NEXTAUTH_SECRET=<retrieved-from-parameter-store>
NEXTAUTH_URL=https://sliot.de

# AWS Configuration
AWS_REGION=eu-north-1

# Note: For production deployment, use the load-parameters.sh script:
# ./load-parameters.sh --deploy